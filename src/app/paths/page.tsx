import { Route } from "lucide-react";
import { PlaceholderPage } from "@/components/ui/placeholder-page";

export default function PathsPage() {
  return <PlaceholderPage title="Path Finder" description="Controls for discovering multi-hop dependency paths will be implemented in a future phase." icon={Route} />;
}

