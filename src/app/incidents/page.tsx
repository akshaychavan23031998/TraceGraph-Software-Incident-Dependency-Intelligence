import { GitBranch } from "lucide-react";
import { PlaceholderPage } from "@/components/ui/placeholder-page";

export default function IncidentsPage() {
  return <PlaceholderPage title="Incident Investigator" description="Connected incident context, affected services, deployments, and resolvers will be implemented in a future phase." icon={GitBranch} />;
}

